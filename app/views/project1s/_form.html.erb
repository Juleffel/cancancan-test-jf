<%= form_for([@company, project1]) do |form| %>
  <% if project1.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(project1.errors.count, "error") %> prohibited this project1 from being saved:</h2>

      <ul>
        <% project1.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :company_id %>
    <%= form.text_field :company_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
